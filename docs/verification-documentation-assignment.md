# Verification Assignment Draft

The current repository contains only scaffolding.

Future verification work should likely group tasks by module:

- `module_1`: MQTT connection lifecycle and simple telemetry
- `module_2`: LED and servo command handling with reported state
- `module_3`: threshold-based local device logic

Verification implementation is expected to live in `mqtt-worker` and corresponding backend persistence paths.
